# Страница нового протокола: экран ошибки вместо формы

## Что видно сейчас (проверено)

- В консоли опубликованного сайта: React #418 и #423 — это несовпадение при «оживлении» уже готовой разметки страницы. Первая ошибка приходит из общего защитного экрана страницы (RouteErrorBoundary), поэтому вместо формы показывается сообщение об ошибке.
- Служебные страницы (`/admin` и всё внутри него) сознательно исключены из предварительной подготовки страниц при сборке — так задумано и меняться не должно.
- В режиме разработки страница нового протокола открывается нормально. Значит, сбой относится только к опубликованной/собранной версии.
- Что именно приходит с сервера по адресу служебной страницы и почему приложение выбирает режим «оживления» вместо обычного запуска — пока не подтверждено чтением. Это первый шаг плана, а не готовый вывод.

## Что я НЕ трогаю

- Настройки сборки, точку входа приложения, резервные адреса backend, файлы авторизации и хранилище сессии — они заморожены.
- Маршруты статей, `/for-parents`, редактор, галереи, окно 18+.
- Ничего не публикую без отдельной команды.

## Шаг 1. Подтвердить причину (без правок)

1. Собрать текущую версию так же, как для сайта, и открыть её локально под моей тестовой учётной записью.
2. Открыть адрес нового протокола и зафиксировать: что реально приходит с сервера, пустая ли заготовка страницы, есть ли в ней метка «страница уже отрисована», и в какой момент срабатывает защитный экран.
3. Показать вам короткий вывод: одна конкретная причина.

## Шаг 2. Одна минимальная правка

Правку выбираю только после шага 1 и показываю вам точный diff. Ожидаемые варианты — ровно один из них:

- **A.** Если приложение ошибочно считает пустую заготовку «уже отрисованной» — снять эту метку для служебных страниц, чтобы приложение запускалось обычным способом. Правка в одном месте.
- **B.** Если причина в том, что разметка первого кадра отличается из-за проверки входа — отложить отрисовку защищённого содержимого до готовности сессии на этой странице. Правка в одном файле страницы/обёртки доступа.
- **C.** Если причина окажется другой — опишу её отдельно и предложу такую же по объёму правку, не выходя за один-два файла.

Ни в одном варианте я не отключаю защитный экран и не «прячу» ошибку.

## Шаг 3. Проверка

- Проверка типов и сборка.
- Открыть собранную версию под входом: страница нового протокола открывается, форма работает, в консоли нет #418/#423.
- Заодно открыть список визитов и одну существующую служебную страницу, чтобы убедиться, что ничего не сломалось.
- Публикация — только после вашей команды.

## Ответ на вопрос

Нет, эту ошибку я ещё не устранял: в прошлый раз я только читал логи и проверял страницу. Правок по ней не вносилось.
